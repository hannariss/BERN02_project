# # Froward stepwise selection
# m0 = LogisticRegression(solver='newton-cg', max_iter=1000)
# m0.fit(np.zeros(len(y_train_f)).reshape(-1,1), y_train_f)

# chosen_cols = []
# p = X_train_f.shape[1]

# for k in range(p):
#     performances = {}
#     for pred in range(p):
#         if pred not in chosen_cols:
#             subset = pd.concat(X_train_f.iloc[:, chosen_cols], X_train_f.iloc[:, pred], axis=1)
#             m = LogisticRegression(solver='newton-cg', max_iter=1000)
#             m.fit(subset, y_train_f)
#             y_pred = m.predict_proba(subset)
#             rss = np.sum((y_train_f - y_pred)**2)
#             performances.update({pred: rss})
#     chosen_cols.append(min(performances, key=performances.get))